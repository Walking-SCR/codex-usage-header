# 安全说明：CDP 调试端口

本插件通过 Chromium DevTools Protocol（CDP）与本机运行的 Codex / ChatGPT
桌面应用通信，默认端口 **9229**，仅监听 `127.0.0.1`（本机回环），不对外暴露。

## 风险

能够连接该端口的**本机任意进程**都可以：

- 列出并操控桌面应用的所有渲染目标（`Runtime.evaluate` 可在页面中执行任意 JS）；
- 读取页面内的 DOM 内容；
- 通过本插件的注入脚本间接触发用量查询。

这意味着：**与你同一台电脑上的恶意软件或不可信的本地程序，理论上可以利用
这个调试端口操控桌面应用**。这是 CDP `--remote-debugging-port` 机制本身的
特性，不是本插件引入的新风险——Chrome/Edge 的远程调试端口有同样的性质。

## 缓解措施（本插件已采取）

- 启动参数固定为 `--remote-debugging-address=127.0.0.1`，绝不绑定 `0.0.0.0`；
- 不在本机开放任何 HTTP 桥接服务（渲染器只通过 CDP 单向接收脱敏快照）；
- 账号与用量数据只保存在本机文件中，不上传到任何服务器；
- 卸载时会停止插件的监控进程并移除注入的组件（`bin/uninstall.sh` / `codex-header --teardown`）。

## 建议

- 不要在不可信的网络环境中把本机端口转发给他人；
- 如需彻底关闭调试通道，直接退出桌面应用即可（端口随应用进程消失）；
- 对安全要求极高的环境，可以不运行本插件——这是可选的增强工具，
  不是桌面应用的必需组件。
